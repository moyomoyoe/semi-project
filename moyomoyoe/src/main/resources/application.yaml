server:
  port: 8080

## driver config
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/moyomoyoedb
    username: moyomoyoe
    password: moyomoyoe

mybatis:
  mapper-locations: classpath:mappers/*.xml  # MyBatis 매퍼 XML 파일 경로
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

